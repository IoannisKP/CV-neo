document.documentElement.classList.add('js');
const reducedMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;
const finePointer=matchMedia('(pointer: fine) and (hover: hover)').matches;
if(finePointer&&!reducedMotion){document.documentElement.classList.add('cursor-enhanced');const c=document.createElement('div');c.className='custom-cursor';c.setAttribute('aria-hidden','true');document.body.appendChild(c);let x=-50,y=-50,cx=-50,cy=-50;const render=()=>{cx+=(x-cx)*.24;cy+=(y-cy)*.24;c.style.transform=`translate3d(${cx-c.offsetWidth/2}px,${cy-c.offsetHeight/2}px,0)`;requestAnimationFrame(render)};addEventListener('pointermove',e=>{x=e.clientX;y=e.clientY;c.classList.add('visible')},{passive:true});document.addEventListener('pointerleave',()=>c.classList.remove('visible'));document.querySelectorAll('[data-cursor]').forEach(t=>{t.addEventListener('pointerenter',()=>{c.dataset.label=t.dataset.cursor||'';c.classList.add('is-project')});t.addEventListener('pointerleave',()=>{c.dataset.label='';c.classList.remove('is-project')})});render()}
if(!reducedMotion&&'IntersectionObserver'in window){const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');o.unobserve(e.target)}}),{rootMargin:'0px 0px -8% 0px',threshold:.08});document.querySelectorAll('.reveal').forEach(e=>o.observe(e))}else document.querySelectorAll('.reveal').forEach(e=>e.classList.add('visible'));
const links=[...document.querySelectorAll('.case-nav a[href^="#"]')];if(links.length&&'IntersectionObserver'in window){const sections=links.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);const o=new IntersectionObserver(es=>{const v=es.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(v)links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${v.target.id}`))},{rootMargin:'-18% 0px -62% 0px',threshold:[0,.1,.4]});sections.forEach(s=>o.observe(s))}
